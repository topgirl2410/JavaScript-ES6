'use strict';
// do while se ejecutara al menos una vez, pero siempre se ejecutara (no importa si se cumple o no la condicion)

let i = 0; // Inicializar 

do {

    console.log(`Número ${i}`)

    i++;  // Incremento
} while (i < 10);  // Condición 