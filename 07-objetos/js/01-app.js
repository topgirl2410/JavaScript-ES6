'use strict';

/**
 * Un objeto es una coleccion de datos y/o funcionalidad relacionados.
 * Estos suelen constar de varias variables y funciones (que se denominan
 * propiedades y métodos cuando estan dentro de objetos).
 */


const nombre = "Monitor 20 pulgadas";
const precio = 450;
const disponible = true;

/** Object Literal **/
const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 450,
    disponible: true
}


console.log(producto);